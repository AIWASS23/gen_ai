# TEMPLATE — não aplique este arquivo como está. Preencha os valores reais
# fora do controle de versão (ex.: via `kubectl create secret generic
# house-price-api-secrets --from-literal=...`, um External Secrets
# Operator, Sealed Secrets, ou o cofre de segredos do seu provedor de
# nuvem) e nunca faça commit de segredos reais em texto plano.
#
# `stringData` (em vez de `data` com base64 manual) só para tornar o
# template legível — o Kubernetes codifica para base64 automaticamente ao
# aplicar.
apiVersion: v1
kind: Secret
metadata:
  name: house-price-api-secrets
  namespace: house-price-prediction
type: Opaque
stringData:
  APP_LANGFUSE_PUBLIC_KEY: "REPLACE_ME"
  APP_LANGFUSE_SECRET_KEY: "REPLACE_ME"
  APP_ADMIN_API_KEY: "REPLACE_ME"
